'use strict';

/* jasmine specs for controller header go here */

describe('controllers', function (){
  beforeEach(module('header'));

  it('should ....', inject(function () {
    //spec body
  }));

  it('should ....', inject(function () {
    //spec body
  }));
});